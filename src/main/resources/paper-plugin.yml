name: ScovilleUI
description: $description
prefix: ScovilleUI
version: '$version'

main: dev.meluhdy.scoville.ScovilleUI
api-version: '1.21.11'
load: POSTWORLD

authors: [ meluwudy ]
website: https://meluhdy.dev

dependencies:
  server:
    Melodia:
      load: BEFORE
      required: true
      join-classpath: true
    ScovilleCore:
      load: BEFORE
      required: true
    ScovilleAchievements:
      load: BEFORE
      required: true
    ScovilleChat:
      load: BEFORE
      required: true